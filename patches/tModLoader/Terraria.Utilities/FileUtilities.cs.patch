--- src/Terraria\Terraria.Utilities\FileUtilities.cs
+++ src/tModLoader\Terraria.Utilities\FileUtilities.cs
@@ -25,10 +_,11 @@
 				SocialAPI.Cloud.Delete(path);
 				return;
 			}
-			if (Utilities.PlatformUtilities.IsWindows)
-				FileOperationAPIWrapper.MoveToRecycleBin(path);
-			else
-				File.Delete(path);
+			/// Apparently doesn't work on universal
+			/// if (PlatformUtilities.IsWindows)
+			///	FileOperationAPIWrapper.MoveToRecycleBin(path);
+			/// else
+			File.Delete(path);
 		}
 
 		public static string GetFullPath(string path, bool cloud)

